import React, { Component, PropTypes } from 'react';

export default class Projects extends Component {
  render() {
    return (
      <div className='projects'>
        This is Projects Page where all project list are found
      </div>
    );
  }
}
